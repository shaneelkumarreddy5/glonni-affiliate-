import { ExternalLink } from 'lucide-react'; import { getStores } from '@/lib/catalog'; import { ScrollRail } from '@/components/scroll-rail';
export async function StoreSection(){const stores=await getStores();return <section className="home-store-section"><div className="section-title"><div><p className="eyebrow">AFFILIATE BRANDS</p><h2>Shop by store</h2></div><a href="/deals">View all stores</a></div><ScrollRail className="home-store-rail" label="stores">{stores.map(store=><a className="store" href={`/store/${store.slug}`} key={store.id}><strong>{store.name.slice(0,1)}</strong><span>{store.name}</span><ExternalLink size={14}/></a>)}</ScrollRail></section>}
