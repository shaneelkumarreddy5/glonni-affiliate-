import { ExternalLink } from 'lucide-react'; import { stores } from '@/lib/mock-data';
export function StoreSection(){return <section><div className="section-title"><div><p className="eyebrow">YOUR FAVOURITE DESTINATIONS</p><h2>Shop by Store</h2></div><a href="#">Explore all</a></div><div className="stores">{stores.map(store=><a className="store" href={`/go/store-${store.name.toLowerCase()}`} key={store.name}><strong style={{background:store.color}}>{store.mark}</strong><span>{store.name}</span><ExternalLink size={14}/></a>)}</div><p className="disclosure">Store links are tracked affiliate destinations. We may earn a commission when you shop, at no extra cost to you.</p></section>}
