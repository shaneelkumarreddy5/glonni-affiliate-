import { ExternalLink } from 'lucide-react'; import { getStores } from '@/lib/catalog'; import { ScrollRail } from '@/components/scroll-rail';
export async function StoreSection(){const stores=await getStores();return <section className="home-store-section"><div className="section-title"><div><p className="eyebrow">AFFILIATE STORES</p><h2>Shop by store</h2></div><a href="/stores">View all stores</a></div>{stores.length?<ScrollRail className="home-store-rail" label="stores">{stores.map(store=><a className="store" href={`/store/${store.slug}?from=/`} key={store.id}>{store.logo_url?<strong className="store-logo"><img src={store.logo_url} alt=""/></strong>:<strong>{store.name.slice(0,1)}</strong>}<span>{store.name}</span><ExternalLink size={14}/></a>)}</ScrollRail>:<div className="home-empty"><b>Stores are being connected</b><span>Available stores will appear here.</span></div>}</section>}
