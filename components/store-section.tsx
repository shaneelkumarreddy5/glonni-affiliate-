import { ExternalLink } from 'lucide-react'; import { getStores } from '@/lib/catalog';
export async function StoreSection(){const stores=await getStores();return <section><div className="section-title"><div><p className="eyebrow">YOUR FAVOURITE DESTINATIONS</p><h2>Shop by Store</h2></div><a href="/deals">Explore all</a></div><div className="stores">{stores.map(store=><a className="store" href={`/store/${store.slug}`} key={store.id}><strong>{store.name.slice(0,1)}</strong><span>{store.name}</span><ExternalLink size={14}/></a>)}</div><p className="disclosure">Store destinations are configuration-driven. Glonni may earn a commission from qualifying purchases, at no extra cost to you.</p></section>}
