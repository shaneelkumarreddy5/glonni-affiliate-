import { signIn, signUp } from './actions'; import { PasswordResetRequest } from '@/components/password-reset-request';
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string;checkEmail?:string;next?:string}>}){const p=await searchParams;const next=p.next?.startsWith('/')&&!p.next.startsWith('//')?p.next:'/account';const message=p.error==='admin'?'This account does not have administrator access. Sign in with admin@glonni.com.':p.error==='recovery'?'That recovery link expired or was already used. Request a fresh link below.':p.error?'We couldn’t complete that request. Please check your details and try again.':'';return <main className="login-page"><a href="/" className="logo"><span>Glonn</span><i>i</i></a><section><p className="eyebrow">YOUR GLONNI ACCOUNT</p><h1>Save the deals you love.</h1><p>Sign in to access saved offers and a personalized deal experience.</p>{message&&<div className="notice">{message}</div>}{p.checkEmail&&<div className="notice success">Check your email to confirm your account.</div>}<form><input type="hidden" name="next" value={next}/><label>Email<input name="email" type="email" required placeholder="you@example.com"/></label><label>Password<input name="password" type="password" minLength={8} required placeholder="At least 8 characters"/></label><button formAction={signIn} className="primary">Sign in</button><button formAction={signUp} className="secondary">Create account</button></form><PasswordResetRequest/><small>By continuing, you agree that Glonni only helps you discover offers; merchants handle purchases, payments and delivery.</small></section></main>}
