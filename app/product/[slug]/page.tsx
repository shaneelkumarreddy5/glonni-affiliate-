import { Header } from '@/components/header'; import { getCatalogOffer } from '@/lib/catalog'; import { notFound } from 'next/navigation';
export const dynamic = 'force-dynamic';
export default async function ProductPage({params}:{params:Promise<{slug:string}>}){const offer=await getCatalogOffer((await params).slug);if(!offer)notFound();return <><Header/><main className="product-page"><a className="back" href="/deals">← Back to deals</a><section className="product-detail"><div className="product-photo"><img src={offer.products?.image_url || ''} alt=""/></div><div><p className="eyebrow">{offer.merchants?.name} · VERIFIED OFFER</p><h1>{offer.products?.title}</h1><p className="muted">Compare this current offer and complete checkout securely on the merchant’s website.</p><div className="price"><b>₹{offer.current_price?.toLocaleString('en-IN')}</b>{offer.list_price&&<del>₹{offer.list_price.toLocaleString('en-IN')}</del>}</div><p className="cashback-big">Earn up to ₹{offer.cashback_amount?.toLocaleString('en-IN')} cashback</p><a className="primary" href={`/go/${offer.id}`}>View deal at {offer.merchants?.name} →</a><p className="disclosure">You will leave Glonni and be redirected to the merchant. We may earn a commission at no additional cost to you.</p></div></section></main></>}
