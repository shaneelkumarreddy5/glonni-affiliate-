import { ArrowRight, BadgeCheck, CircleHelp, ShieldCheck, Sparkles } from 'lucide-react';
import { Header } from '@/components/header'; import { StoreSection } from '@/components/store-section'; import { HomeOfferRail } from '@/components/home-offer-rail'; import { ScrollRail } from '@/components/scroll-rail'; import { getCatalogOffers,getCategories } from '@/lib/catalog'; import './home.css';
export const dynamic='force-dynamic'; const icons=['📱','🎧','👕','🏠','💄','🏸','🛒','⚡','💻','🧸','🧳','🧴'];
export default async function Home(){const [offers,categories]=await Promise.all([getCatalogOffers(),getCategories()]);const priceDrops=[...offers].sort((a,b)=>((b.list_price??0)-(b.current_price??0))-((a.list_price??0)-(a.current_price??0)));const trending=[...offers].sort((a,b)=>(b.current_price??0)-(a.current_price??0));return <><Header/><main className="home-main"><ScrollRail className="home-banner-rail" label="featured banners"><article className="home-banner"><p>FEATURED DEALS</p><h2>Compare before you shop.</h2><span>Find the right deal across configured stores, in one clean place.</span><a href="/deals">Explore deals <ArrowRight size={15}/></a><div className="banner-icon">🛍️</div><div className="banner-chip">Compare store offers</div></article><article className="home-banner"><p>SEASONAL PICKS</p><h2>Fresh finds for every cart.</h2><span>Explore fashion, tech, beauty and everyday essentials.</span><a href="/deals">Browse categories <ArrowRight size={15}/></a><div className="banner-icon">✨</div><div className="banner-chip">New curated picks</div></article><article className="home-banner"><p>ELIGIBLE CASHBACK</p><h2>Rewards only where approved.</h2><span>See exact cashback on the offers that actually support it.</span><a href="/deals">Find eligible offers <ArrowRight size={15}/></a><div className="banner-icon">₹</div><div className="banner-chip">Offer-specific benefit</div></article></ScrollRail><ScrollRail className="home-category-row" label="categories">{categories.map((c,i)=><a href={`/deals?category=${c.slug}`} className="home-category" key={c.id}><span>{icons[i%icons.length]}</span><b>{c.name}</b></a>)}</ScrollRail><StoreSection/><section id="deals"><div className="section-title"><div><p className="eyebrow">BEST DEALS</p><h2>Best deals right now</h2></div><a href="/deals">View all deals</a></div><HomeOfferRail offers={offers.slice(0,10)} bestDeal/></section><section><div className="section-title"><div><p className="eyebrow">TRENDING NOW</p><h2>Popular picks across stores</h2></div><a href="/deals">View all trending deals</a></div><HomeOfferRail offers={trending.slice(0,10)}/></section><section><div className="section-title"><div><p className="eyebrow">PRICE DROPS</p><h2>Worth a closer look</h2></div><a href="/deals">View all price drops</a></div><HomeOfferRail offers={priceDrops.slice(0,10)}/></section><section className="benefits">{[[ShieldCheck,'Trusted & Secure','You purchase directly from the merchant'],[BadgeCheck,'Compare Stores','Price, coupon and reward in one view'],[Sparkles,'Eligible Cashback','Only on offers marked eligible'],[CircleHelp,'Help When You Need','Clear support whenever you need it']].map(([Icon,title,body])=>{const I=Icon as typeof ShieldCheck;return <div key={String(title)}><I/><p><b>{String(title)}</b><small>{String(body)}</small></p></div>})}</section></main></>}
