import { createClient } from '@/lib/supabase/server'; import { redirect } from 'next/navigation';
export default async function Account(){const supabase=await createClient();const {data}=await supabase.auth.getClaims();if(!data?.claims)redirect('/login');return <main className="account-page"><a href="/" className="logo"><span>Glonn</span><i>i</i></a><section><p className="eyebrow">WELCOME TO GLONNI</p><h1>Your account is ready.</h1><p>Saved deals and personalized discovery will be added here in the next customer experience increment.</p><a className="primary" href="/deals">Explore current deals →</a></section></main>}
