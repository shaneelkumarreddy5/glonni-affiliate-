import { updatePassword } from '@/app/login/actions';
export default async function ResetPassword({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="login-page"><a href="/" className="logo"><span>Glonn</span><i>i</i></a><section><p className="eyebrow">SECURE ACCOUNT RECOVERY</p><h1>Choose a new password.</h1><p>After saving, you’ll be returned to the admin workspace.</p>{p.error&&<div className="notice">We couldn’t update the password. Please request a new link and try again.</div>}<form action={updatePassword}><label>New password<input name="password" type="password" minLength={8} required placeholder="At least 8 characters"/></label><button className="primary">Save password</button></form></section></main>}
