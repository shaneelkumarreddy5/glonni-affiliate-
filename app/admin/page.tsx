import { createClient } from '@/lib/supabase/server'; import { addCategory,addProduct,addStore,removeItem } from './actions';
export const dynamic='force-dynamic';
export default async function Admin(){const s=await createClient();const [categories,stores,products,offers]=await Promise.all([s.from('categories').select('id,name').order('display_order'),s.from('merchants').select('id,name').order('homepage_position'),s.from('products').select('id,title,brand').order('created_at',{ascending:false}),s.from('offers').select('id,current_price,products(title),merchants(name)').order('created_at',{ascending:false})]);const Remove=({table,id}:{table:string;id:string})=><form action={removeItem}><input type="hidden" name="table" value={table}/><input type="hidden" name="id" value={id}/><button className="text-action">Remove</button></form>;return <main className="admin"><aside><a className="logo" href="/"><span>Glonn</span><i>i</i></a><p>ADMIN CONSOLE</p><a className="active" href="/admin">Catalog manager</a><a href="/deals">View deals</a></aside><section className="admin-body"><header><div><p className="eyebrow">ADMIN WORKSPACE</p><h1>Catalog manager</h1><small className="role-pill">Public edit mode</small></div><a href="/" className="admin-link">View storefront →</a></header><article className="audit"><h2>Mock catalog controls</h2><p>Live providers remain disabled. This public manager must be secured with login before real data is added.</p></article><section className="admin-workspace"><article><div className="panel-head"><h2>Categories</h2></div><form action={addCategory} className="stack-form"><input name="name" required placeholder="Category name"/><button>Add category</button></form><div className="admin-list">{categories.data?.map(x=><p key={x.id}><b>{x.name}</b><Remove table="categories" id={x.id}/></p>)}</div></article><article><div className="panel-head"><h2>Stores</h2></div><form action={addStore} className="stack-form"><input name="name" required placeholder="Store name"/><input name="url" required type="url" placeholder="Storefront URL"/><button>Add store</button></form><div className="admin-list">{stores.data?.map(x=><p key={x.id}><b>{x.name}</b><Remove table="merchants" id={x.id}/></p>)}</div></article><article><div className="panel-head"><h2>Products</h2></div><form action={addProduct} className="stack-form"><input name="title" required placeholder="Product title"/><input name="brand" placeholder="Brand"/><select name="category"><option value="">No category</option>{categories.data?.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><button>Add product</button></form><div className="admin-list">{products.data?.map(x=><p key={x.id}><b>{x.title}</b><span>{x.brand}</span><Remove table="products" id={x.id}/></p>)}</div></article><article><div className="panel-head"><h2>Offers</h2></div><p className="muted">Offer editing will be added after the catalog structure is set.</p><div className="admin-list">{offers.data?.map(x=><p key={x.id}><b>{(x.products as {title?:string}|null)?.title}</b><span>{(x.merchants as {name?:string}|null)?.name} · ₹{x.current_price}</span><Remove table="offers" id={x.id}/></p>)}</div></article></section></section></main>}
