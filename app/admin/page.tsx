import { createClient } from '@/lib/supabase/server';
import { addCategory,addOffer,addProduct,addStore,removeItem } from './actions';

export const dynamic='force-dynamic';

export default async function Admin(){
  const s=await createClient();
  const [categories,stores,products,offers]=await Promise.all([
    s.from('categories').select('id,name').order('display_order'),
    s.from('merchants').select('id,name').order('homepage_position'),
    s.from('products').select('id,title,brand').order('created_at',{ascending:false}),
    s.from('offers').select('id,current_price,reward_type,cashback_amount,cashback_percent,coupon_code,products(title),merchants(name)').order('created_at',{ascending:false})
  ]);
  const Remove=({table,id}:{table:string;id:string})=><form action={removeItem}><input type="hidden" name="table" value={table}/><input type="hidden" name="id" value={id}/><button className="text-action">Remove</button></form>;
  return <main className="admin"><aside><a className="logo" href="/"><span>Glonn</span><i>i</i></a><p>ADMIN CONSOLE</p><a className="active" href="/admin">Catalog manager</a><a href="/deals">View deals</a></aside><section className="admin-body"><header><div><p className="eyebrow">ADMIN WORKSPACE</p><h1>Catalog & offer manager</h1><small className="role-pill">Public edit mode</small></div><a href="/" className="admin-link">View storefront →</a></header><article className="audit"><h2>Clear reward rules</h2><p>Set a customer reward per offer. Choose “No customer reward” for Amazon-style offers that may earn Glonni a commission but must not promise the shopper cashback. Live providers remain disabled.</p></article><section className="admin-workspace"><article><div className="panel-head"><h2>Categories</h2></div><form action={addCategory} className="stack-form"><input name="name" required placeholder="Category name"/><button>Add category</button></form><div className="admin-list">{categories.data?.map(x=><p key={x.id}><b>{x.name}</b><Remove table="categories" id={x.id}/></p>)}</div></article><article><div className="panel-head"><h2>Stores</h2></div><form action={addStore} className="stack-form"><input name="name" required placeholder="Store name"/><input name="url" required type="url" placeholder="Storefront URL"/><button>Add store</button></form><div className="admin-list">{stores.data?.map(x=><p key={x.id}><b>{x.name}</b><Remove table="merchants" id={x.id}/></p>)}</div></article><article><div className="panel-head"><h2>Products</h2></div><form action={addProduct} className="stack-form"><input name="title" required placeholder="Product title"/><input name="brand" placeholder="Brand"/><input name="image" type="url" placeholder="Image URL"/><select name="category"><option value="">No category</option>{categories.data?.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><button>Add product</button></form><div className="admin-list">{products.data?.map(x=><p key={x.id}><b>{x.title}</b><span>{x.brand}</span><Remove table="products" id={x.id}/></p>)}</div></article><article><div className="panel-head"><h2>Add an offer</h2></div><form action={addOffer} className="stack-form"><select name="product" required><option value="">Product</option>{products.data?.map(x=><option value={x.id} key={x.id}>{x.title}</option>)}</select><select name="merchant" required><option value="">Store</option>{stores.data?.map(x=><option value={x.id} key={x.id}>{x.name}</option>)}</select><input name="url" type="url" required placeholder="Destination URL"/><input name="price" type="number" required placeholder="Current price"/><input name="listPrice" type="number" placeholder="List price"/><label className="form-label">Customer benefit<select name="rewardType" defaultValue="none"><option value="none">No customer reward — best price only</option><option value="fixed_cashback">Fixed Glonni Cashback</option><option value="percentage_cashback">Percentage Glonni Cashback</option><option value="coupon">Coupon code</option><option value="merchant_promotion">Merchant promotion</option></select></label><input name="cashback" type="number" placeholder="Fixed cashback amount (₹)"/><input name="cashbackPercent" type="number" step="0.01" placeholder="Cashback percentage (%)"/><input name="cashbackCap" type="number" placeholder="Maximum cashback (₹)"/><input name="couponCode" placeholder="Coupon code (if applicable)"/><select name="fundingSource"><option value="provider">Provider-funded cashback</option><option value="merchant">Merchant-funded cashback</option><option value="glonni">Glonni-funded cashback</option></select><input name="rewardTerms" placeholder="Example: tracked in 48 hrs; confirmed after 30 days"/><small className="form-help">Only select a cashback reward if that exact provider/merchant offer supports customer cashback tracking.</small><button>Add offer</button></form><div className="admin-list">{offers.data?.map(x=>{const benefit=x.reward_type==='fixed_cashback'?`₹${x.cashback_amount} Glonni Cashback`:x.reward_type==='percentage_cashback'?`${x.cashback_percent}% Glonni Cashback`:x.reward_type==='coupon'?`Code: ${x.coupon_code}`:x.reward_type==='merchant_promotion'?'Merchant promotion':'Best price only';return <p key={x.id}><b>{(x.products as {title?:string}|null)?.title}</b><span>{(x.merchants as {name?:string}|null)?.name} · ₹{x.current_price} · {benefit}</span><Remove table="offers" id={x.id}/></p>})}</div></article></section></section></main>;
}
