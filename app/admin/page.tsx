import { Activity, Bot, Boxes, CircleDollarSign, LayoutDashboard, Settings, Store, Tags, Users } from 'lucide-react';
const cards=[['Live offers','0','Ready for approved provider data'],['Tracked clicks','0','Event collection ready'],['Active stores','0','Configuration-driven'],['Agent mode','Manual','No automated actions']];
export default function Admin(){return <main className="admin"><aside><div className="logo"><span>Glonn</span><i>i</i></div><p>ADMIN CONSOLE</p>{[[LayoutDashboard,'Overview'],[Boxes,'Products'],[Tags,'Offers'],[Store,'Stores'],[CircleDollarSign,'Providers'],[Bot,'AI Control Center'],[Activity,'Marketing Control'],[Users,'Users'],[Settings,'Settings']].map(([I,label],i)=>{const Icon=I as typeof LayoutDashboard;return <a className={i===0?'active':''} key={String(label)}><Icon size={18}/>{String(label)}</a>})}</aside><section className="admin-body"><header><div><p className="eyebrow">STEP 1 FOUNDATION</p><h1>Good morning, Glonni team</h1></div><button>Manual mode</button></header><div className="admin-cards">{cards.map(([title,value,body])=><article key={title}><small>{title}</small><h2>{value}</h2><p>{body}</p></article>)}</div><div className="admin-panels"><article><div><h2>AI Agent Control Center</h2><span>All agents are disabled by default</span></div>{['Deal discovery','Product normalization','Offer quality','Paid marketing'].map((agent,i)=><p key={agent}><Bot size={17}/><b>{agent}</b><em>{i===3?'Budget controls enforced':'Manual review required'}</em><button>Manual</button></p>)}</article><article><div><h2>Provider framework</h2><span>Provider-neutral integrations</span></div><p className="empty">No providers connected.<br/>Add an approved API, feed or manual-link adapter when permission is available.</p><button className="add">Add provider</button></article></div><article className="audit"><h2>Audit & event stream</h2><p>Tracking clicks, changes and automated actions will appear here. Every event is designed to retain actor, time, source and metadata.</p></article></section></main>}
