import { AdminSidebar } from '@/components/admin-sidebar'; import { Bell, Gift, Image, LayoutTemplate, Tag } from 'lucide-react'; import { createClient } from '@/lib/supabase/server'; import { campaignStatus, createCampaign } from '../campaign-actions';
export const dynamic='force-dynamic';
export default async function CampaignsPage(){const s=await createClient();const[{data:campaigns},{data:offers}]=await Promise.all([s.from('homepage_campaigns').select('id,title,placement,device,status,display_order,offers(products(title),merchants(name),reward_type)').order('display_order'),s.from('offers').select('id,reward_type,products(title),merchants(name)').eq('status','active').order('created_at',{ascending:false})]);const list=campaigns??[],active=(offers??[]).filter(x=>x.reward_type==='fixed_cashback'||x.reward_type==='percentage_cashback').length;return <main className="admin-v2"><AdminSidebar/><section className="admin-main"><header className="admin-top"><Gift size={21}/><b>Deals &amp; Banners</b><span className="dashboard-date">Homepage content operations</span><Bell size={19}/><span className="avatar">SR</span></header><main className="admin-content"><div className="admin-title"><div><p>CAMPAIGN MANAGEMENT</p><h1>Deals &amp; Banners</h1><span>Manage separately configured desktop and mobile homepage placements.</span></div><a className="add-store" href="#new-campaign">＋ Create campaign</a></div><p className="preview-note">A published campaign is customer-visible. Cashback labels are derived from the attached offer, never typed as a store-wide claim.</p><section className="admin-stats"><article><Tag/><div><small>Campaigns</small><b>{list.length}</b><em>Homepage records</em></div></article><article><Gift/><div><small>Cashback offers</small><b>{active}</b><em>Eligible only</em></div></article><article><Image/><div><small>Hero slots</small><b>2</b><em>Desktop + mobile</em></div></article><article><LayoutTemplate/><div><small>Placements</small><b>6</b><em>Configurable</em></div></article></section><section className="admin-grid"><article className="store-table"><div className="table-head"><div><a className="current">All campaigns</a><a>Draft</a><a>Published</a><a>Paused</a></div></div><div className="table-scroll"><table><thead><tr><th>CAMPAIGN</th><th>PLACEMENT</th><th>DEVICE</th><th>ATTACHED OFFER</th><th>STATUS</th><th>ACTIONS</th></tr></thead><tbody>{list.map(c=>{const offer=c.offers as unknown as {products?:{title?:string},merchants?:{name?:string}}|null;return <tr key={c.id}><td><strong>{c.title}</strong></td><td>{c.placement.replaceAll('_',' ')}</td><td>{c.device}</td><td>{offer?.products?.title?`${offer.products.title} · ${offer.merchants?.name??'Store'}`:'Banner only'}</td><td><em className={c.status==='published'?'status-active':'status-paused'}>{c.status}</em></td><td><form action={campaignStatus}><input type="hidden" name="id" value={c.id}/><input type="hidden" name="action" value={c.status==='draft'?'submit':c.status==='pending'?'approve':c.status==='approved'?'publish':'pause'}/><button className="table-action">{c.status==='draft'?'Submit':c.status==='pending'?'Approve':c.status==='approved'?'Publish':'Pause'}</button></form></td></tr>})}</tbody></table></div></article></section><section id="new-campaign" className="quick-add"><div><p>HOMEPAGE CAMPAIGN</p><h2>Create a controlled placement</h2><span>Draft first, then submit, approve and publish.</span></div><form action={createCampaign}><input name="title" required placeholder="Campaign title"/><select name="placement"><option value="hero">Hero banner</option><option value="top_deals">Top deals</option><option value="trending">Trending</option><option value="price_drops">Price drops</option><option value="cashback_picks">Cashback picks</option><option value="collection">Collection</option></select><select name="device"><option value="all">Desktop + mobile</option><option value="desktop">Desktop only</option><option value="mobile">Mobile only</option></select><select name="offer"><option value="">Banner only</option>{(offers??[]).map(o=>{const p=o.products as unknown as {title?:string}|null;const m=o.merchants as unknown as {name?:string}|null;return <option value={o.id} key={o.id}>{p?.title??'Product'} · {m?.name??'Store'}</option>})}</select><input name="imageUrl" type="url" placeholder="Creative image URL (optional)"/><input name="cta" placeholder="CTA label"/><input name="displayOrder" type="number" min="1" placeholder="Homepage order"/><button>Create draft</button></form></section></main></section></main>}
