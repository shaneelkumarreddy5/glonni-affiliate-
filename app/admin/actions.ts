'use server';
import { revalidatePath } from 'next/cache'; import { createClient } from '@/lib/supabase/server';
const slug=(v:string)=>v.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'');const refresh=()=>{revalidatePath('/admin');revalidatePath('/');revalidatePath('/deals');};
export async function addCategory(f:FormData){const name=String(f.get('name')??'').trim();if(name){const s=await createClient();await s.from('categories').insert({name,slug:slug(name),display_order:99,is_active:true});refresh();}}
export async function addStore(f:FormData){const name=String(f.get('name')??''),url=String(f.get('url')??'');if(name&&url){const s=await createClient();await s.from('merchants').insert({name,slug:slug(name),storefront_url:url,homepage_position:99,is_active:true});refresh();}}
export async function addProduct(f:FormData){const title=String(f.get('title')??'');if(title){const s=await createClient();await s.from('products').insert({title,slug:slug(title),brand:String(f.get('brand')??''),image_url:String(f.get('image')??'')||null,category_id:String(f.get('category')??'')||null,is_active:true});refresh();}}
export async function addOffer(f:FormData){const product_id=String(f.get('product')??''),merchant_id=String(f.get('merchant')??''),destination_url=String(f.get('url')??''),reward_type=String(f.get('rewardType')??'none');const valid=['none','fixed_cashback','percentage_cashback','coupon','merchant_promotion'];if(product_id&&merchant_id&&destination_url&&valid.includes(reward_type)){const s=await createClient();const cashback=reward_type==='fixed_cashback'?Number(f.get('cashback'))||null:null;const percent=reward_type==='percentage_cashback'?Number(f.get('cashbackPercent'))||null:null;await s.from('offers').insert({product_id,merchant_id,destination_url,current_price:Number(f.get('price')),list_price:Number(f.get('listPrice'))||null,reward_type,cashback_amount:cashback,cashback_percent:percent,cashback_cap:percent?Number(f.get('cashbackCap'))||null:null,coupon_code:reward_type==='coupon'?String(f.get('couponCode')??'').trim()||null:null,reward_terms:String(f.get('rewardTerms')??'').trim()||null,cashback_tracking_supported:['fixed_cashback','percentage_cashback'].includes(reward_type),reward_funding_source:['fixed_cashback','percentage_cashback'].includes(reward_type)?String(f.get('fundingSource')??'provider'):'none',status:'active'});refresh();}}
export async function removeItem(f:FormData){const table=String(f.get('table')),id=String(f.get('id'));if(['categories','merchants','products','offers'].includes(table)&&id){const s=await createClient();await s.from(table).delete().eq('id',id);refresh();}}
