import { adminUpdatePassword } from '@/app/admin/auth-actions';
export default async function Page({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <main className="admin-auth-page"><section className="admin-auth-card"><p className="eyebrow">ADMIN RECOVERY</p><h1>Choose a new password</h1>{error && <div className="auth-notice error">{error}</div>}<form action={adminUpdatePassword} className="auth-form"><label>New password<input type="password" name="password" minLength={12} required placeholder="At least 12 characters"/></label><button>Update password</button></form></section></main> }
