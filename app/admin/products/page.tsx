import { AdminSidebar } from '@/components/admin-sidebar';
import { Bell, Boxes, CheckCircle2, FolderTree, Store, Tag } from 'lucide-react';
import { createClient } from '@/lib/supabase/server';
import { addProduct, removeItem } from '../actions';
export const dynamic='force-dynamic';
export default async function ProductsPage(){const s=await createClient();const[{data:products},{data:categories},{count:offerCount}]=await Promise.all([s.from('products').select('id,title,slug,brand,is_active,categories(name)').order('title'),s.from('categories').select('id,name').eq('is_active',true).order('display_order'),s.from('offers').select('*',{count:'exact',head:true}).eq('status','active')]);return <main className="admin-v2"><AdminSidebar/><section className="admin-main"><header className="admin-top"><Boxes size={21}/><b>Products</b><span className="dashboard-date">Normalized product catalogue</span><Bell size={19}/><span className="avatar">SR</span></header><main className="admin-content"><div className="admin-title"><div><p>CATALOGUE MANAGEMENT</p><h1>Products</h1><span>One product record can hold offers from multiple merchants and providers.</span></div><a className="add-store" href="#new-product">＋ Add product</a></div><p className="preview-note">Products are provider-neutral. Store price, coupons and customer cashback belong to individual offers.</p><section className="admin-stats"><article><Boxes/><div><small>Products</small><b>{products?.length??0}</b><em>Normalized records</em></div></article><article><Tag/><div><small>Active offers</small><b>{offerCount??0}</b><em>Across merchants</em></div></article><article><Store/><div><small>Multi-store ready</small><b>Yes</b><em>Product → offers</em></div></article><article><FolderTree/><div><small>Categories</small><b>{categories?.length??0}</b><em>Discovery-ready</em></div></article><article><CheckCircle2/><div><small>Quality review</small><b>Ready</b><em>Before publishing</em></div></article></section><section className="admin-grid"><article className="store-table"><div className="table-head"><div><a className="current">All products</a><a>Active</a><a>Missing offer</a></div></div><div className="table-scroll"><table><thead><tr><th>PRODUCT</th><th>BRAND</th><th>CATEGORY</th><th>URL HANDLE</th><th>STATUS</th><th>ACTION</th></tr></thead><tbody>{(products??[]).map(p=><tr key={p.id}><td><span><strong>{p.title}</strong><small>Normalized product record</small></span></td><td>{p.brand||'Unbranded'}</td><td>{(p.categories as {name?:string}|null)?.name||'Uncategorised'}</td><td>/product/{p.slug}</td><td><em className={p.is_active?'status-active':'status-paused'}>{p.is_active?'Active':'Inactive'}</em></td><td><form action={removeItem}><input type="hidden" name="table" value="products"/><input type="hidden" name="id" value={p.id}/><button className="table-action danger-text">Remove</button></form></td></tr>)}</tbody></table></div><footer>Products remain provider-neutral; offers contain merchant-specific terms.</footer></article></section><section id="new-product" className="quick-add"><div><p>PRODUCT SETUP</p><h2>Add a normalized product</h2><span>Then add one or more merchant offers under it.</span></div><form action={addProduct}><input name="title" required placeholder="Product title"/><input name="brand" placeholder="Brand"/><select name="category"><option value="">Choose category</option>{(categories??[]).map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select><input name="image" type="url" placeholder="Image URL (optional)"/><input name="description" placeholder="Short description"/><button>Add product</button></form></section></main></section></main>}
