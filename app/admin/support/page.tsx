import { AdminSidebar } from '@/components/admin-sidebar';
import { Bell, Headphones, MessageSquareText, ShieldCheck, TicketCheck } from 'lucide-react';
import { createClient } from '@/lib/supabase/server';
import { replyToSupportTicket } from './actions';
export default async function SupportPage(){const s=await createClient();const{data:tickets}=await s.from('support_tickets').select('id,ticket_number,subject,category,status,priority,updated_at,profiles(display_name)').order('updated_at',{ascending:false}).limit(50);const all=tickets??[];const open=all.filter(t=>['open','in_review'].includes(t.status)).length;return <main className="admin-v2"><AdminSidebar/><section className="admin-main"><header className="admin-top"><Headphones size={21}/><b>Support Centre</b><span className="dashboard-date">AI-first customer support · human escalation</span><Bell size={19}/><span className="avatar">SR</span></header><main className="admin-content"><div className="admin-title"><div><p>SUPPORT &amp; TRUST</p><h1>Customer Support Centre</h1><span>Customers receive approved FAQ answers first. Sensitive, uncertain, and high-risk matters become reviewable human tickets.</span></div></div><p className="preview-note">Support email: <b>support@glonni.com</b> · Never request passwords, OTPs, or full bank details through a ticket.</p><section className="admin-stats"><article><TicketCheck/><div><small>Open queue</small><b>{open}</b><em>Needs an owner or team reply</em></div></article><article><MessageSquareText/><div><small>All requests</small><b>{all.length}</b><em>Live Supabase records</em></div></article><article><ShieldCheck/><div><small>High-risk boundary</small><b>Human</b><em>Money, security and account changes</em></div></article></section><section className="admin-grid"><article className="store-table"><div className="table-head"><div><a className="current">Escalated requests</a><a>Open</a><a>In review</a><a>Resolved</a></div></div><div className="table-scroll"><table><thead><tr><th>TICKET</th><th>CUSTOMER</th><th>CATEGORY</th><th>STATUS</th><th>UPDATED</th><th>REPLY</th></tr></thead><tbody>{all.map(t=>{const p=t.profiles as unknown as {display_name?:string|null}|null;return <tr key={t.id}><td><strong>#{t.ticket_number} · {t.subject}</strong></td><td>{p?.display_name||'Customer'}</td><td>{t.category}</td><td><em className={t.status==='resolved'?'status-active':'status-paused'}>{t.status.replaceAll('_',' ')}</em></td><td>{new Date(t.updated_at).toLocaleDateString('en-IN')}</td><td><form action={replyToSupportTicket} className="review-action"><input type="hidden" name="ticketId" value={t.id}/><input name="body" required minLength={2} placeholder="Reply to customer"/><select name="status" defaultValue="waiting_on_customer"><option value="in_review">In review</option><option value="waiting_on_customer">Waiting on customer</option><option value="resolved">Resolve</option></select><button>Send</button></form></td></tr>})}</tbody></table></div><footer>Every customer reply stays connected to the same ticket and is visible in My Space.</footer></article><aside className="admin-right"><article><h2>Assistant safeguards</h2><p>Approved FAQs only <span>Active</span></p><p>Cashback decisions <span>Human only</span></p><p>Wallet movement <span>Human only</span></p><p>Account security <span>Human only</span></p></article><article><h2>FAQ ownership</h2><p className="activity">CMS Help Centre<small>General policy and account guidance</small></p><p className="activity">Store details<small>Merchant-specific cashback rules</small></p><p className="activity">Offer details<small>Exact eligibility and exclusions</small></p></article></aside></section></main></section></main>}
