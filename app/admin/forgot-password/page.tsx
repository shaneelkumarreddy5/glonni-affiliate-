import Link from 'next/link';
import { adminRequestPasswordReset } from '@/app/admin/auth-actions';
export default async function Page({ searchParams }: { searchParams: Promise<{ error?: string; success?: string }> }) { const { error, success } = await searchParams; return <main className="admin-auth-page"><section className="admin-auth-card"><p className="eyebrow">ADMIN RECOVERY</p><h1>Reset admin password</h1><p>We will send a secure five-minute recovery link to the verified work email.</p>{error && <div className="auth-notice error">{error}</div>}{success && <div className="auth-notice success">{success}</div>}<form action={adminRequestPasswordReset} className="auth-form"><label>Work email<input type="email" name="email" required/></label><button>Send recovery link</button></form><Link className="auth-back" href="/admin/login">← Admin sign in</Link></section></main> }
