import Link from 'next/link';
export default function Page() { return <main className="admin-auth-page"><section className="admin-auth-card"><p className="eyebrow">ADMIN ACCESS RECOVERY</p><h1>Owner-controlled recovery</h1><p>Self-service email recovery is disabled for Admin because email security scanners can consume one-time links. Employees request a private reset from the Owner. The Owner uses a short-lived setup link created through the secured project workflow.</p><div className="auth-notice success">No password or recovery token is sent by email.</div><Link className="auth-back" href="/admin/login">← Admin sign in</Link></section></main> }
