import { Building2 } from 'lucide-react';
import { HrPageShell } from '@/components/hr-page-shell';import { createClient } from '@/lib/supabase/server';import { createDepartment } from '@/app/admin/hr/actions';
export default async function Page({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){const params=await searchParams;const supabase=await createClient();const{data:departments}=await supabase.from('departments').select('*').order('name');return <HrPageShell title="Departments" subtitle="Define accountability, ownership and reporting structure across Glonni.">{params.error&&<p className="preview-note error-note">{params.error}</p>}{params.success&&<p className="preview-note success-note">{params.success}</p>}<section className="hr-layout"><article className="store-table"><div className="table-head"><div><a className="current">All departments</a></div></div><table><thead><tr><th>DEPARTMENT</th><th>CODE</th><th>PURPOSE</th><th>STATUS</th></tr></thead><tbody>{(departments??[]).map(d=><tr key={d.id}><td><Building2 size={18}/><span><strong>{d.name}</strong></span></td><td>{d.code}</td><td>{d.description}</td><td><em className={d.is_active?'status-active':'status-paused'}>{d.is_active?'Active':'Paused'}</em></td></tr>)}</tbody></table></article><aside className="hr-form-card"><p className="eyebrow">ORGANIZATION</p><h2>Add department</h2><form action={createDepartment} className="support-form"><label>Name *<input name="name" required/></label><label>Code *<input name="code" maxLength={20} required placeholder="EXAMPLE"/></label><label>Description<textarea name="description" rows={4}/></label><button className="primary">Create department</button></form></aside></section></HrPageShell>}
