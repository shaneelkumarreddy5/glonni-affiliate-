import { UserPlus } from 'lucide-react';
import { HrPageShell } from '@/components/hr-page-shell';
import { createClient } from '@/lib/supabase/server';
import { inviteEmployee } from '@/app/admin/hr/actions';

export default async function NewEmployee(){const supabase=await createClient();const[{data:departments},{data:employees},{data:profiles}]=await Promise.all([supabase.from('departments').select('id,name').eq('is_active',true).order('name'),supabase.from('employees').select('profile_id,work_email,status').neq('status','departed'),supabase.from('profiles').select('id,display_name')]);const names=new Map((profiles??[]).map(p=>[p.id,p.display_name]));return <HrPageShell title="Invite Employee" subtitle="Create a complete HR record and send secure invitation-only access."><section className="employee-form-page"><div className="hr-form-title"><UserPlus/><div><p className="eyebrow">NEW EMPLOYEE</p><h2>Employment and access details</h2></div></div><form action={inviteEmployee} className="employee-form"><fieldset><legend>Personal &amp; contact</legend><label>Full legal name *<input name="displayName" autoComplete="name" required/></label><label>Work email *<input name="email" type="email" autoComplete="email" required/></label><label>Mobile number<input name="phone" type="tel" autoComplete="tel" placeholder="+91"/></label></fieldset><fieldset><legend>Employment</legend><label>Employee ID<span>Generated automatically after acceptance</span><input value="GL-AUTO" disabled/></label><label>Job title *<input name="jobTitle" required/></label><label>Department<select name="departmentId"><option value="">Unassigned</option>{(departments??[]).map(d=><option value={d.id} key={d.id}>{d.name}</option>)}</select></label><label>Reporting manager<select name="managerId"><option value="">No manager / Owner</option>{(employees??[]).map(e=><option value={e.profile_id} key={e.profile_id}>{names.get(e.profile_id)??e.work_email}</option>)}</select></label><label>Employment type<select name="employmentType"><option value="full_time">Full time</option><option value="part_time">Part time</option><option value="contractor">Contractor</option><option value="intern">Intern</option></select></label><label>Joining date<input name="joiningDate" type="date"/></label></fieldset><fieldset><legend>Admin security &amp; authority</legend><label>Role *<select name="role"><option value="editor">Team Member</option><option value="admin">Administrator</option><option value="owner">Owner</option></select></label><label>Financial approval limit (₹)<input name="approvalLimit" type="number" min="0" step="1" defaultValue="0"/></label><div className="security-checklist"><b>Automatically enforced</b><span>✓ Verified work-email invitation</span><span>✓ Private password chosen by employee</span><span>✓ Mandatory Google Authenticator-compatible 2FA</span><span>✓ New-device password and 2FA challenge</span><span>✓ Complete login and action audit trail</span></div></fieldset><div className="form-actions"><a href="/admin/team">Cancel</a><button className="primary">Create record &amp; send invitation</button></div></form></section></HrPageShell>}
