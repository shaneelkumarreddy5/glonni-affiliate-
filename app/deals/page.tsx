import { Header } from '@/components/header'; import { OfferGrid } from '@/components/offer-grid'; import { getCatalogOffers,getCategories,getStores } from '@/lib/catalog';
export const dynamic='force-dynamic';
export default async function DealsPage({searchParams}:{searchParams:Promise<{q?:string;store?:string;category?:string}>}){const filters=await searchParams;const [offers,stores,categories]=await Promise.all([getCatalogOffers(filters),getStores(),getCategories()]);const title=filters.q?`Results for “${filters.q}”`:filters.store?`Deals from ${stores.find(x=>x.slug===filters.store)?.name??'this store'}`:filters.category?`${categories.find(x=>x.slug===filters.category)?.name??'Category'} deals`:'Deals worth a closer look';return <><Header/><main><section className="catalog-head deals-head"><p className="eyebrow">DEAL DISCOVERY</p><h1>{title}</h1><p>{offers.length} mock offers from configured stores. Compare price, coupon and cashback eligibility before selecting an offer.</p></section><form className="catalog-search" action="/deals"><input name="q" defaultValue={filters.q} placeholder="Search products or brands"/><button className="primary">Search</button></form><div className="filter-row"><a href="/deals">All deals</a>{categories.map(c=><a className={filters.category===c.slug?'selected':''} href={`/deals?category=${c.slug}`} key={c.id}>{c.name}</a>)}</div><div className="filter-row store-filters"><span>Shop by store</span>{stores.map(s=><a className={filters.store===s.slug?'selected':''} href={`/store/${s.slug}`} key={s.id}>{s.name}</a>)}</div><OfferGrid offers={offers}/>{!offers.length&&<div className="empty-state"><h2>No matching mock offers</h2><p>Try another product, brand, store or category.</p><a href="/deals" className="primary">See all deals</a></div>}</main></>}
